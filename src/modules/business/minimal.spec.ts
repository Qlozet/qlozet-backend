import { BusinessService } from './business.service';

describe('MinimalBusinessTest', () => {
    it('should import BusinessService', () => {
        expect(BusinessService).toBeDefined();
    });
});
